# v4 Spec Conformance Matrix

This matrix tracks implementation against `docs/masked_team_league_system_v4_detailed.tex`.
Status meanings:

- `done`: implemented with tests and used by the main runtime path.
- `partial`: implemented as an interface, skeleton, heuristic, or unit-tested helper but not the documented runtime path.
- `missing`: not implemented.

| Spec area | Tex section | Required behavior | Current status | Gap to close |
|---|---:|---|---|---|
| Core data objects | 142-331 | `MatchFormat`, `HeroRecord`, `Loadout`, `Team`, `AttackPlan`, `DefensePlan`, `Observation` carry loadout-level data, version, season, rank segment, hashes. | done | Core dataclasses, metadata, hashes, structured diagnostics, run metadata, core tables, legality diagnostics, and machine-readable output contract registry exist. Runtime/training report contracts now cover oracle outputs, daily reports, active-query feedback, production readiness, ablation reports/plans, training schedule/status/scheduler state, real-calibration reports, run metadata, core tables, and legality diagnostics. |
| Legality engine | 358-470, 1498-1509, 1693-1714 | Structural legality, hidden domains, MRV/forward checking, future feasible action masks, rejection of illegal inputs. | done | Unit tests, future-feasible proposal-network action masks, optional goal/reference-cost budget filtering, formal 20 legal attacks / 20 legal defenses / 20 illegal inputs acceptance pack, structured rejection diagnostics, and `docs/legality_diagnostics_schema.md` exist. |
| Utility and underdog | 472-556 | DP match win probability, resource cost, attack/defense underdog gaps, residual objective, target power ratios 1.0/0.9/0.8. | partial | DP/cost exists. Underdog goal is used by attack/defense role generation with fallback when budget is infeasible, active query reports carry underdog priority, and neural proposal masks filter by target budget when goal/reference cost are supplied. AttackOracle and DefenseOracle now add explicit underdog residual objective scoring, stable risk fields, report aggregates, ablation metrics, and scheduled `underdog_residual_validation_report.v1` red-line validation. The live underdog residual smoke is intentionally not production-ready yet: attack underdog rows have zero mean residual bonus and defense underdog rows are absent, and the recurring scheduler stops on those red lines. Remaining gap: run larger production rounds that clear the underdog residual quality gate. |
| SingleTeamWinrateModel | 557-663, 2254-2272 | Position-aware model with loadout encoder, ordered team encoder, 5x5 cross interaction, win/uncertainty/margin/time/residual heads, ensemble calibration metrics. | partial | Torch model, scorer adapter, ensemble scorer, save/load, streaming JSONL loader/training/eval API, CLI training script, post-training calibration pass, holdout report, checkpoint registry, split manifests, checkpoint selection, v4 schedule dry-run/execute orchestration, CPU/memory/GPU resource snapshots, and recurring scheduler daemon state exist. Remaining gap: long-running production deployment validation. |
| Proposal network rules | 665-720 | Causal decoder generation, legal action mask distinct from causal mask, search distillation. | partial | Torch causal pointer decoder, legal mask helpers, non-causal context encoder, BC/value/gap/anti-meta residual distillation loss, attack/defense teacher sample builders, JSONL artifact loaders, grouped multi-candidate teacher weighting, role/target residual weighting for exploiters and underdogs, optimizer loop, checkpoint/registry integration, mask-respecting beam/sample token API, attack/defense proposal training CLIs, multi-round self-play proposal feedback loop, defense attack-meta context tensors, v4 schedule wiring, resource snapshots, and recurring scheduler daemon exist. Need long-running production deployment validation. |
| AttackGenerationNetwork | 721-827, 1737-1758 | Observation/belief/pool/state/goal input, causal transformer decoder, pointer head, legal mask, value head, BC/value/gap losses. | partial | Causal pointer/value/gap module, generic observation/belief/pool context encoder, attack teacher builder/loader with grouped candidate weighting, target-defense context, exploiter residual target, role weighting, active-real feedback propagation, distillation trainer, beam/sample token API, generated-token-to-AttackPlan adapter, AttackOracle candidate-source hook, trained checkpoint candidate-source loader, runtime observation/belief/pool context tensor adapter, self-play checkpoint feedback into AttackOracle, `exploiter_effectiveness_report.v1` with round trend validation, combined `learned_exploiter_validation_report.v1` red-line gating, and per-round self-play validation/early-stop loop exist. Attack teacher residuals now use same-defense main attack success as the baseline instead of the all-role defense break rate, so exploiters are not compared against themselves. A live 1-round validation-loop smoke and early-stop smoke cleared the learned exploiter/anti-meta gate with 9 oracle requests and no red lines, but a 3-round top50 learned smoke with 27 oracle requests produced negative attack anti-meta residual and failed the production gate. Need stronger attack best-response sampling/candidate budget and longer production-scale learned exploiter runs that clear the same gate. |
| DefenseRosterGenerationNetwork | 828-923, 1759-1780 | Attack meta encoder, causal roster decoder, anti-meta scorer, legal mask, defense value head. | partial | Causal pointer/value/anti-meta residual module, defense attack-meta context tensors, defense teacher builder/loader, training CLI, checkpoint loader with legacy compatibility, DefenseOracle roster-source hook, multi-round self-play checkpoint feedback, explicit anti-meta residual teacher targets, `defense_anti_meta_effectiveness_report.v1`, scheduled defense anti-meta red-line validation, combined learned exploiter/anti-meta validation gating, and per-round self-play validation/early-stop loop exist. A live 1-round validation-loop smoke produced a defense proposal checkpoint and defense feedback; the combined gate cleared at smoke scale. Need stronger production-scale anti-meta/learned-exploiter runs that clear the same gate. |
| MaskSelectionNetwork | 924-993, 1781-1802 | Slot scorer plus constrained selector, ambiguity/domain/leakage/counter-sensitivity objective, BO3 enumerate and BO5 beam, ranking loss. | partial | Torch slot scorer plus constrained selector exists; `MaskSearcher`/`DefenseOracle` consume learned slot-score providers; mask training samples now include slot/loadout, ambiguity, break-rate, learned-score, and counter-sensitivity features; BCE+ranking-loss training, checkpoint save/load provider, CLI, schedule job, slot-level learned mask explanations, high-hidden-priority bounded mask enumeration, and `mask_explanation_validation_report.v1` with training-schedule/daemon red-line integration exist. A 16-worker live smoke with `max_masks_per_roster=1` validated 6 hidden slots and 100% hidden-slot explanation coverage. Need longer production validation. |
| BeliefModel | 994-1053, 1803-1824 | Build legal completions from observation, output weighted candidates, entropy, feasible count, gap, domain stats; AttackOracle only scores legal candidates. | partial | Legal completion belief plus DefensePool retrieval, RealMetaDB exact-observation and similar-observation retrieval, neural ranker adapter, artifact-backed ranker training/eval job, checkpoint loading, round-artifact ranker dataset builder, split manifest, registry-based ranker selection, runner injection, scheduled round-artifact ranker retraining in the v4 training schedule, aggregate domain/weight stats, candidate artifact `belief_domain_stats`, and scheduled `belief_real_distribution_validation_report.v1` red-line validation exist. A live smoke validated 100% real-distribution coverage and zero oracle-alignment MAE on exact real-meta belief candidates. Need longer production validation that real-distribution similarity weighting improves belief quality. |
| AttackOracle | 1054-1120, 1825-1846 | Complete and mask-aware search with mixed candidate sources, diversity, successive halving, risk report, simulation re-evaluation. | partial | Mask-aware path is used by the strict runtime round, external candidate-source hooks allow proposal networks to feed candidates before scoring/diversity/halving, structured risk reports include expected lane rates, worst-case belief completion, backup attack scores, `attack_oracle_output.v1` JSON contract, domain-specific failure annotations for belief/candidate/halving failures, and scheduled `attack_oracle_failure_validation_report.v1` red-line validation. Appendix G tests now explicitly verify full-defense top-5 attack legality and mask-observation belief candidate compatibility. A smoke validated one annotated failure output plus 3 normal real-round candidate risk reports with 100% annotation/diagnostic coverage. Need longer production validation. |
| DefenseOracle | 1120-1168, 1847-1868 | Generate roster, choose mask, call AttackOracle on observation, evaluate worst attack on true roster, output break risk and explanation. | partial | Runtime uses DefenseOracle -> observation -> AttackOracle, attack meta affects heuristic scoring, trained defense proposal checkpoints can feed roster candidates before mask search, bounded mask search now prioritizes hidden masks instead of all-visible masks, defense risk reports include break/survival rates, worst attack, backups, hidden count, counter-attack risk, and mask explanations with hidden slot scores/loadout details, and `defense_oracle_output.v1` JSON contract exists. Appendix G tests now explicitly verify DefenseOracle counter-attack success reporting. Need production-scale anti-meta validation. |
| ActivePerceptionScheduler | 1169-1234, 1869-1890 | Schedule sim and real queries using info gain, decision impact, meta frequency, novelty, underdog, cost. | partial | Scoring helper is integrated into league rounds via enriched `active_queries.jsonl`, `oracle_pairs.jsonl`, daily reports, feedback reports, `dispatch_active_real_queries.py`, `active_real_query_dispatch_validation.v1`, scheduled `active_query_feedback_report.v1`, and scheduled aggregate `active_real_query_dispatch_validation_report.v1` red-line validation. Self-play appends real-query attack/defense teacher rows into training, and recurring production readiness can now verify that active real-query dispatches produced completed oracle requests and teacher feedback. Appendix G tests now explicitly verify high-info/high-decision-impact query priority. A 16-worker live-backend smoke validated one real query, 3 simulator requests, 0 oracle errors, complete teacher feedback, and 100% active-query feedback coverage; a scaled active-real smoke validated 2 real queries, 6 simulator dispatch requests, 0 oracle errors, complete teacher feedback, and `active_real_query_dispatch_validation_report.v1 production_ready=true`. Need larger production-scale queue validation. |
| RealCalibrationModel and RealMetaDB | 1235-1283, 1891-1912 | Real schema, calibration model, real-distribution belief, version drift. | partial | JSONL-persistent RealMetaDB schema, Platt calibrator, real-context feature extraction, feature-weight calibrator, time decay, exact and similar RealMetaDB belief weighting, league-round artifact ingestion job, active real-query feedback ingestion job, round/active-real artifact -> calibration sample builder, feature calibration CLI/report, holdout `real_calibration_validation_report.v1` Brier/ECE gate, season/version drift report, recurring scheduler ingestion/sample-build/validation stages, and drift/red-line stops exist. A synthetic holdout smoke validated Brier improvement 0.34 and ECE improvement 0.30 through direct/schedule/daemon paths. A real-artifact schedule smoke built 4 calibration samples, ingested 4 RealMetaRecords, and passed the holdout validation gate. Need production-scale real holdout validation. |
| LeagueManager PSRO | 1284-1380, 1913-1934 | Attack/defense pools, payoff matrix, meta distribution, main/exploiter/underdog roles, historical retention, league iteration loop. | partial | Pool/payoff, mixed meta, main/exploiter/underdog role loop, active historical retention, strength-based exploiter target APIs, runner retention wiring, multi-round attack/defense proposal self-play orchestration, validation-gated self-play early-stop, and scheduled `league_selfplay_health_report.v1` now exist. The league health gate validates pool sizes, active retention, required role coverage, diversity clusters, payoff density, and current-iteration strength deltas, and participates in recurring scheduler and production-readiness red lines. Appendix G tests now explicitly verify that 10 League iterations do not collapse attack/defense pools into a single hero cluster. A 3-round top50 smoke reached attack pool 9, defense pool 3, and payoff density 0.1111, but failed defense role coverage in the 1-defense/round setup. Still needs stronger learned exploiter training feedback and role-balanced self-play runs that clear the combined gate at production scale. |
| Data engineering | 1380-1415 | Canonical hashes, cache keys with versions/seed policy, core tables, reproducibility metadata. | partial | Hash/cache implemented in memory, split manifests, `run_metadata.v1` reproducibility manifest with artifact hashing/config hashes, `LeagueRoundRunner`/`run_training_schedule()` manifest emission, `core_tables.v1` JSONL schema for Loadout/SingleMatchup/PlanMatch/Observation/LeagueStrategy tables, round-level `tables/*.jsonl` emission, league strategies carry resource cost/active-retired state/real hero-set diversity clusters, and scheduled `data_engineering_validation_report.v1` red-line validation now checks run metadata coverage, artifact hashes, and core table coverage. A 2-round real-artifact smoke reported 100% metadata/hash/table coverage and no red lines. Need longer production long-run validation of metadata/table stability. |
| Training workflow | 1416-1439 | Six stages: coverage, single model, AttackOracle teachers, DefenseOracle teachers, league self-play, real calibration. | partial | Single-team value training entry point, holdout report, checkpoint registry, attack/defense/mask teacher training, proposal distillation, multi-round proposal/self-play orchestration with optional real-query dispatch feedback, attack exploiter target/residual teacher feedback, `run_training_schedule.py` dry-run/execute job graph, active-query feedback, active-real dispatch feedback validation, active-real feedback -> RealMetaDB ingestion, round/active-real feedback -> real calibration sample building, data-engineering metadata/table validation, underdog residual validation, League/PSRO health validation, AttackOracle failure, exploiter, defense anti-meta, combined learned exploiter validation, auto-collected production readiness aggregation, mask explanation, belief real-distribution validation, and real calibration holdout validation jobs, resource snapshots, recurring scheduler daemon with persistent state, failure stop, red-line stop, recurring real calibration ingestion/sample-build/drift/holdout validation stages, and iteration-local report red-line ingestion exist. Need production long-run validation. |
| Evaluation protocol | 1440-1467, 2365-2405 | AUC/Brier/ECE/Precision@K/Recall@K, oracle top-K, defense metrics, league metrics, ablations, daily report, red lines. | partial | Metric helpers, daily report JSON, league round report CLI, active-query feedback/dispatch report artifacts with red lines, active-real dispatch validation report/CLI, data-engineering validation report/CLI, underdog residual validation report/CLI, League/PSRO health report/CLI, AttackOracle failure validation report/CLI, exploiter effectiveness report/CLI with per-round trend and red lines, defense anti-meta effectiveness report/CLI with per-round trend and red lines, combined learned exploiter validation report/CLI, real calibration validation report/CLI, `production_readiness_report.v1` aggregate gate/CLI with optional same-schedule report auto-collection and nested real-calibration ingestion schema counting, `v4_conformance_validation_report.v1` requirement-level evidence matrix/CLI/schedule gate for the five production priorities, mask explanation validation report/CLI, belief real-distribution validation report/CLI, scheduled red-line checks, scheduler job wiring for fixed report output, risk-aware domain report fields, v4 ablation report CLI, and v4 ablation experiment plan/execution CLI exist. Production-readiness direct/schedule/daemon smokes cleared clean report sets, correctly blocked known underdog residual red lines, and now auto-collected active-query/active-real-dispatch/data-engineering/real-calibration validation outputs in schedule and daemon smokes. All required v4 ablation variants now have executable controls: no-position, no-equipment-stars, no-future-feasibility-mask, no-underdog, no-mask-ambiguity, no-real-calibration, and no-active-perception. A 16-worker live full-variant smoke ran all 8 variants with 0 oracle errors and no missing variants. Need production-scale full-variant ablation runs and production feedback validation. |
| Real oracle backend path | 1494-1497, 2378-2380 | Decoupled backend, persistent workers, true simulation results, real platform closed loop. | partial | Backend adapter, strict mask-observation role-loop small round, oracle artifact -> RealMetaDB ingestion, active real-query dispatch feedback, active real-query feedback -> RealMetaDB ingestion, round/active-real feedback -> real calibration samples, and aggregate active-real dispatch validation work. The latest 16-worker live smoke wrote `exports/active_real_query_validation_smoke/round_0001`, uncached real-query feedback with 3/3 completed requests, and a scheduled/daemon `active_real_query_dispatch_validation_report.v1` gate with `production_ready=true`. Heroes 45 and 75 are excluded by default after stable oracle failure isolation. Remaining gap: production-scale real-platform calibration loop with large holdouts and repeated drift validation. |
| Main round runtime | 1356-1375, 1468-1497 | League iteration: update payoffs, compute meta, generate main/exploiter/underdog attacks and defenses, schedule queries. | partial | `LeagueRoundRunner` advances league iteration, uses DefenseOracle, mask observation, default main/exploiter/underdog attack/defense roles, true-oracle verification, active query artifact output, full plan blobs for proposal teachers, structured attack/defense risk artifacts, run metadata, and proposal checkpoints as attack/defense candidate sources. `SelfPlayOrchestrator` now runs multi-round attack/defense feedback and can validate learned exploiter/anti-meta quality after each round, recording report paths/red lines and stopping early when the gate clears. Remaining gap is learned exploiter quality. |

## Immediate correction priorities

1. Run production-scale learned exploiter / anti-meta loops until `learned_exploiter_validation_report.v1` and the aggregate `production_readiness_report.v1` clear attack and defense red lines.
2. Run production-scale full v4 ablation experiments and production feedback validation.
3. Run production-scale real feature calibration holdout validation on true real-meta data and feed it into the aggregate production readiness gate.
4. Scale live real-query dispatch validation beyond the current 1-query smoke now that `active_real_query_dispatch_validation_report.v1` feeds recurring training reports.
5. Add production-scale validation for learned mask/risk explanations and belief real-distribution weighting.
